<nav class="navbar navbar-expand-lg navbar-light bg-light Regular shadow" style="background: linear-gradient(-180deg, #FAD961 0%, #F5A623 100%);">
  <a class="navbar-brand" href="/"><%= image_tag "outils.png", height: 50 %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <!-- Artisan -->
      <% if current_user.nil?  %>
        <li class="nav-item" style="margin-top: 20px; margin-bottom: 10px">
          <i class="fas fa-sign-in-alt" style="color: rgba(0, 0, 0, .5); margin-right: 10px;"></i>
          <%= link_to 'Connexion', new_user_session_path, class:"nav-link d-inline" %>
        </li>
      <% elsif current_user.admin %>
        <li class="nav-item" style="margin-top: 20px; margin-bottom: 10px">
          <i class="fas fa-user-alt" style="color: rgba(0, 0, 0, .5); margin-right: 10px;"></i>
          <%= link_to "Mon profil", edit_user_registration_path, class:"nav-link d-inline" %>
        </li>
        <li class="nav-item" style="margin-bottom: 10px">
          <i class="fas fa-file-alt" style="color: rgba(0, 0, 0, .5); margin-right: 14px;"></i>
          <%= link_to "Nouvel Article", new_article_path, class:"nav-link d-inline" %>
        </li>
        </li>
        <li class="nav-item">
          <i class="fas fa-sign-out-alt" style="color: rgba(0, 0, 0, .5); margin-right: 10px;"></i>
          <%= link_to 'Deconnexion', destroy_user_session_path, class:"nav-link d-inline", method: :delete %>
        </li>
      <!-- User -->
      <% else %>
        <li class="nav-item" style="margin-top: 20px; margin-bottom: 10px">
          <i class="fas fa-user-alt" style="color: rgba(0, 0, 0, .5); margin-right: 10px;"></i>
          <%= link_to "Mon profil", edit_user_path, class:"nav-link d-inline" %>
        </li>
        <li class="nav-item">
          <i class="fas fa-sign-out-alt" style="color: rgba(0, 0, 0, .5); margin-right: 10px;"></i>
          <%= link_to 'Deconnexion', destroy_user_session_path, class:"nav-link d-inline", method: :delete %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
